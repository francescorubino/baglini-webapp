define(['backbone'], function(Backbone) {
    var CommentoModel = Backbone.Model.extend({

        defaults: {
//            name: "empty name"
        }

    });
    return CommentoModel;
});