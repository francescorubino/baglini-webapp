define(['backbone'], function(Backbone) {
    var ArticoloModel = Backbone.Model.extend({

        defaults: {
//            name: "empty name"
        }

    });
    return ArticoloModel;
});